mkdir -p data

docker network create --driver bridge prometheus

docker run -d --network=prometheus -v $(pwd)/prometheus-config:/config -v $(pwd)/data:/prometheus --name prometheus-server -p 9090:9090  prom/prometheus:latest -config.file=/config/prometheus.yml

docker run -d --network=prometheus -p 9100:9100 --name nodeexporter prom/node-exporter:latest

docker run -d --network=prometheus -p 9115:9115 --rm --name blackboxprober --privileged -v $(pwd)/blackbox-config/config.yml:/etc/blackbox_exporter/config.yml prom/blackbox-exporter:latest